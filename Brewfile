# Configure and generate the native CMake build.
brew "cmake"

# Provide grealpath for syncProj.sh and the upstream helper build scripts.
brew "coreutils"

# Provide Node.js and its bundled npm for JavaScript tooling.
brew "node"

# Install Visual Studio Code as the optional development editor.
cask "visual-studio-code"
